# Doubly linked lists

Project for practicing implementation of a doubly linked list in C.

- Print all elements of a doubly linked list
- Get the number of elements in a list
- Add nodes at the beginning and the end of a list
- Free a list
- Get a node at a given index
- Sum all data in a list
- Insert a node at a given index
- Delete a node at a given index
