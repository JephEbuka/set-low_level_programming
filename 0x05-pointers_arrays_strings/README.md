# 0x05-pointers_arrays_strings

Pointer, array, and string practice in C: updating ints via pointers, swapping values, custom string length and copy routines, and printing strings or parts of strings using `_putchar` and `printf` as required. [page:8]
